{
  "name": "@onedaygames/leaderboard",
  "version": "0.1.0",
  "description": "AWS leaderboard service for OneDayGames",
  "license": "MIT",
  "type": "commonjs",
  "scripts": {
    "test": "jest",
    "build": "tsc -p tsconfig.json",
    "sam-build": "sam build",
    "sam-deploy": "sam deploy --guided"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.590.0",
    "@aws-sdk/client-ssm": "^3.590.0",
    "@aws-sdk/lib-dynamodb": "^3.590.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.134",
    "@types/jest": "^29.5.12",
    "esbuild": "^0.21.5",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.1",
    "typescript": "^5.5.4"
  }
}
